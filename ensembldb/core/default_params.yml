module_dir:
  module_test:
    main: test/
    slurm_scripts: test/slurm_scripts
    slurm_logs: test/slurm_logs
    logs: test/logs
  module_mkidx:
    slurm_scripts: index/slurm_scripts
    slurm_logs: index/slurm_logs
    logs: index/logs
    main: index/
    star: index/star_index
    

module_cmd:
  module_test:
    TestRun: 'echo "hello world."'
  module_mkidx:
    MkindexExtr: 'gffread {t.genome_gtf} -g {t.genome_fa} -w {t.genome_exon_fa}|
                  gffread {t.genome_gtf} -g {t.genome_fa} -x {t.genome_cds_fa}|
                  gffread {t.genome_gtf} -g {t.genome_fa} -y {t.genome_pep_fa}'
  


